"use client";
import { useState, useEffect } from 'react';
import { usePathname } from 'next/navigation';

const TimePopup = () => {
  // 완전히 비활성화 - 항상 null 반환
  return null;
};

export default TimePopup;
